# Volta Commands

## Shell Completions

```powershell
volta completions -o "volta_completions_powershell.txt"   "powershell"
volta completions -o "volta_completions_zsh.txt"          "zsh"
```

## Extract Toolchain

```powershell
volta list --format plain > volta_list.txt
```
